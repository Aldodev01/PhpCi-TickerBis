import React from "react";
import { useParams } from "react-router-dom";

const DetailHistory = () => {
  const { id } = useParams();
  return <div>DETAIL PAKET</div>;
};

export default DetailHistory;
